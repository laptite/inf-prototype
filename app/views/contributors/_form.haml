= form_with model: contributor do |f|
	.input-group
		= f.label 'Name'
		= f.text_field :name

	.input-group
		= f.label 'Bio'
		= f.text_field :bio

	= f.submit 'Save'
	- unless contributor.new_record?
		= button_to 'Delete', contributor_path, method: :delete, data: { confirm: 'Are you sure you want to delete this Contributor?'}